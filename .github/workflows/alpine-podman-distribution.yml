# ============================================================================
# GitHub Actions Workflow для создания DESQEMU дистрибутивов
# ============================================================================
#
# 🏗️ Этот workflow создает ТРИ независимых типа артефактов:
#
# 1️⃣ ПОРТАТИВНЫЕ АРХИВЫ (create-qemu-portable-archive):
#    📦 Самодостаточные архивы с QEMU бинарниками
#    🔧 Установочные скрипты для быстрого развертывания
#    🚀 Используются для создания микро-ВМ без Docker
#
# 2️⃣ ALPINE ДИСТРИБУТИВ (create-alpine-distribution):
#    🐳 Docker образы с Alpine Linux + Podman
#    📁 Rootfs архивы для chroot окружений
#    🔄 Совместимы с различными контейнерными платформами
#
# 3️⃣ ALPINE QCOW2 ОБРАЗЫ (create-alpine-qcow2-images):
#    💿 QCOW2 образы для QEMU виртуальных машин
#    🐧 Готовые к загрузке Alpine Linux ВМ
#    🔧 Создаются с помощью alpine-make-vm-image
#
# ⚡ Джобы выполняются ПАРАЛЛЕЛЬНО для ускорения сборки
# 🔄 Четвертая джоба (create-portable-release) зависит от первой
# ============================================================================

name: Создание Alpine дистрибутива с Podman

# When to run this workflow / Когда запускать этот workflow
on:
  push:
    branches: [master]
    paths:
      - ".github/workflows/alpine-podman-distribution.yml"
  pull_request:
    branches: [master]
    paths:
      - ".github/workflows/alpine-podman-distribution.yml"
  workflow_dispatch: # Manual trigger / Ручной запуск
    inputs:
      alpine_version:
        description: "Версия Alpine Linux"
        required: false
        default: "3.19"
        type: string
      architecture:
        description: "Архитектура"
        required: false
        default: "x86_64"
        type: choice
        options:
          - "x86_64"
          - "aarch64"
          - "arm64"
          - "amd64"

env:
  ALPINE_VERSION: ${{ github.event.inputs.alpine_version || '3.19' }}

jobs:


  create-alpine-distribution:
    name: Создание Alpine дистрибутива (Docker + Rootfs + QCOW2 + Portable + Desktop)
    runs-on: ubuntu-latest
    strategy:
      matrix:
        architecture: [x86_64]
        include:
          - architecture: x86_64
            platform: linux/amd64
    permissions:
      contents: write
      packages: write
    steps:
      - name: 📥 Получение кода репозитория | Get repository code
        uses: actions/checkout@v4

      - name: 🔧 Установка зависимостей | Install dependencies
        run: |
          sudo apt-get update
          sudo apt-get install -y wget curl xz-utils qemu-utils fdisk e2fsprogs dosfstools

      - name: 🔧 Установка Node.js | Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: '18'
          cache: 'npm'
          cache-dependency-path: desqemu-desktop/package.json

      - name: 🖥️ Сборка Desktop приложения | Build desktop app
        run: |
          echo "🔨 Начинаем сборку Desktop приложения..."
          cd desqemu-desktop
          
          echo "📋 Проверяем структуру проекта:"
          ls -la
          
          echo "📦 Устанавливаем зависимости..."
          npm install
          npm install -g @neutralinojs/neu
          
          echo "🔄 Обновляем Neutralino..."
          neu update
          
          echo "🏗️ Собираем приложение..."
          neu build --release
          
          echo "📋 Проверяем результат сборки:"
          ls -la dist/
          
          # Создание архива для распространения
          echo "📦 Создаем архив..."
          cd dist
          tar -czf "../../desqemu-desktop-linux-x64.tar.gz" .
          cd ..
          
          # Проверяем, что QCOW2 образ добавлен
          if [ -f "resources/qcow2/alpine-bootable.qcow2" ]; then
            echo "✅ QCOW2 образ найден в Desktop App"
          else
            echo "⚠️ QCOW2 образ еще не добавлен (будет добавлен позже)"
          fi
          
          echo "📋 Проверяем созданный архив:"
          ls -la ../desqemu-desktop-linux-x64.tar.gz
          du -h ../desqemu-desktop-linux-x64.tar.gz
          
          echo "✅ Desktop приложение собрано"

      - name: 📦 Подготовка ресурсов для Desktop App | Prepare resources for desktop app
        run: |
          echo "📦 Подготавливаем ресурсы для Desktop приложения..."
          
          # Создаем директорию qcow2 в resources
          mkdir -p desqemu-desktop/resources/qcow2
          
          # Копируем QCOW2 образ (если он уже создан)
          if [ -f "desqemu-alpine-qcow2-${{ matrix.architecture }}.qcow2" ]; then
            echo "💿 Копируем QCOW2 образ в Desktop App..."
            cp "desqemu-alpine-qcow2-${{ matrix.architecture }}.qcow2" "desqemu-desktop/resources/qcow2/alpine-bootable.qcow2"
            echo "✅ QCOW2 образ скопирован: $(du -h desqemu-desktop/resources/qcow2/alpine-bootable.qcow2 | cut -f1)"
          else
            echo "⚠️ QCOW2 образ еще не создан, будет добавлен позже"
          fi
          
          # Копируем noVNC скрипт
          echo "🌐 Копируем noVNC скрипт..."
          cp "desqemu-desktop/start-novnc-proxy.sh" "desqemu-desktop/resources/start-novnc-proxy.sh"
          chmod +x "desqemu-desktop/resources/start-novnc-proxy.sh"
          echo "✅ noVNC скрипт скопирован"
          
          # Создаем простой README для qcow2
          echo "# DESQEMU Alpine QCOW2 Образ" > "desqemu-desktop/resources/qcow2/README.md"
          echo "" >> "desqemu-desktop/resources/qcow2/README.md"
          echo "QCOW2 образ с полной настройкой DESQEMU" >> "desqemu-desktop/resources/qcow2/README.md"
          echo "" >> "desqemu-desktop/resources/qcow2/README.md"
          echo "SSH: ssh desqemu@localhost -p 2222 (пароль: desqemu)" >> "desqemu-desktop/resources/qcow2/README.md"
          echo "VNC: localhost:5900 (пароль: desqemu)" >> "desqemu-desktop/resources/qcow2/README.md"
          echo "noVNC: http://localhost:6900" >> "desqemu-desktop/resources/qcow2/README.md"
          
          echo "✅ Ресурсы подготовлены для Desktop приложения"

      - name: 🐳 Настройка Docker Buildx | Setup Docker Buildx
        uses: docker/setup-buildx-action@v3

      - name: 🔨 Подготовка Dockerfile для CI/CD | Prepare Dockerfile for CI/CD
        run: |
          echo "🐳 Подготавливаем Dockerfile для CI/CD..."

          # Update Dockerfile with correct Alpine version
          # Обновляем Dockerfile с правильной версией Alpine
          sed -i "s/FROM alpine:3.19/FROM alpine:${{ env.ALPINE_VERSION }}/" Dockerfile

          # Update labels with correct version
          # Обновляем метки с правильной версией
          sed -i "s/version=\"3.19\"/version=\"${{ env.ALPINE_VERSION }}\"/" Dockerfile

      - name: 🏗️ Сборка Alpine образа | Build Alpine image
        run: |
          echo "🔨 Собираем кастомный Alpine образ для ${{ matrix.platform }}..."
          echo "📋 Отладочная информация:"
          echo "   • matrix.architecture: ${{ matrix.architecture }}"
          echo "   • matrix.platform: ${{ matrix.platform }}"
          echo "   • env.ALPINE_VERSION: ${{ env.ALPINE_VERSION }}"
          
          IMAGE_NAME="ghcr.io/${{ github.repository_owner }}/desqemu-alpine"
          
          # Проверяем, что platform определена
          if [ -z "${{ matrix.platform }}" ]; then
            echo "❌ Ошибка: matrix.platform не определена!"
            exit 1
          fi
          
          docker buildx build --platform ${{ matrix.platform }} --target final-image \
            -t desqemu-alpine:latest \
            -t "$IMAGE_NAME:latest" \
            --load .

          echo "✅ Сборка завершена"
          echo "📋 Проверяем созданные образы:"
          docker images | grep desqemu-alpine

      - name: 🚀 Пуш в GitHub Container Registry | Push to GitHub Container Registry
        run: |
          echo "🚀 Пушим образ в GitHub Container Registry..."

          # Login to GitHub Container Registry using GITHUB_TOKEN
          # Логинимся в GitHub Container Registry используя GITHUB_TOKEN
          echo "${{ secrets.GITHUB_TOKEN }}" | docker login ghcr.io -u ${{ github.actor }} --password-stdin

          # Tag image for GitHub Container Registry
          # Обвешиваем тегом образ для GitHub Container Registry
          IMAGE_NAME="ghcr.io/${{ github.repository_owner }}/desqemu-alpine"

          # Build and push with platform-specific tags
          # Собираем и пушим с платформо-специфичными тегами
          docker buildx build --platform ${{ matrix.platform }} --target final-image \
            -t "$IMAGE_NAME:latest" \
            -t "$IMAGE_NAME:${{ env.ALPINE_VERSION }}" \
            -t "$IMAGE_NAME:${{ env.ALPINE_VERSION }}-${{ matrix.architecture }}" \
            --push .

          echo "✅ Образ опубликован в ghcr.io/${{ github.repository_owner }}/desqemu-alpine"
          echo "📥 Для скачивания: docker pull ghcr.io/${{ github.repository_owner }}/desqemu-alpine:latest"

      - name: 📦 Экспорт в rootfs для chroot | Export to rootfs for chroot
        run: |
          echo "📦 Экспортируем образ в rootfs..."

          # Use the locally built image instead of pulling from GHCR
          # Используем локально собранный образ вместо скачивания из GHCR
          IMAGE_NAME="desqemu-alpine:latest"

          # Check if image exists
          # Проверяем существование образа
          if ! docker image inspect "$IMAGE_NAME" >/dev/null 2>&1; then
            echo "❌ Образ $IMAGE_NAME не найден!"
            echo "📋 Доступные образы:"
            docker images
            exit 1
          fi

          echo "✅ Образ $IMAGE_NAME найден"

          # Create container and export its filesystem to a temporary directory
          # Создаем контейнер и экспортируем его файловую систему во временную директорию
          mkdir -p rootfs-temp
          CONTAINER_ID=$(docker create "$IMAGE_NAME")
          docker export $CONTAINER_ID | tar -C rootfs-temp -x
          docker rm $CONTAINER_ID

          # Create rootfs archive (exclude virtual filesystems)
          # Создаем архив rootfs (исключаем виртуальные файловые системы)
          cd rootfs-temp
          tar -czf "../desqemu-alpine-rootfs-${{ env.ALPINE_VERSION }}-${{ matrix.architecture }}.tar.gz" \
            --exclude='./dev/*' \
            --exclude='./proc/*' \
            --exclude='./sys/*' \
            --exclude='./tmp/*' \
            --exclude='./run/*' \
            --exclude='./mnt/*' \
            --exclude='./media/*' \
            --exclude='./.dockerenv' \
            .
          cd ..

          # Clean up temporary files / Очищаем временные файлы
          rm -rf rootfs-temp

          echo "✅ Rootfs создан: $(du -h desqemu-alpine-rootfs-*.tar.gz | cut -f1)"

      - name: 💾 Сохранение Docker образа | Save Docker image
        run: |
          echo "🐳 Сохраняем Docker образ..."
          IMAGE_NAME="desqemu-alpine:latest"

          # Check if image exists
          # Проверяем существование образа
          if ! docker image inspect "$IMAGE_NAME" >/dev/null 2>&1; then
            echo "❌ Образ $IMAGE_NAME не найден!"
            echo "📋 Доступные образы:"
            docker images
            exit 1
          fi

          echo "✅ Образ $IMAGE_NAME найден"
          docker save "$IMAGE_NAME" | gzip > desqemu-alpine-docker-${{ env.ALPINE_VERSION }}-${{ matrix.architecture }}.tar.gz
          echo "✅ Docker образ сохранен: $(du -h desqemu-alpine-docker-*.tar.gz | cut -f1)"

      - name: 📚 Создание документации | Create documentation
        run: |
          echo "📚 Создаем документацию и скрипты..."
          chmod +x scripts/create-all-docs.sh
          scripts/create-all-docs.sh ${{ env.ALPINE_VERSION }} ${{ matrix.architecture }} ${{ github.repository_owner }}

      - name: ⬆️ Docker образ → GitHub Artifacts | Upload Docker image to GitHub Artifacts
        uses: actions/upload-artifact@v4
        with:
          name: desqemu-alpine-docker-${{ env.ALPINE_VERSION }}-${{ matrix.architecture }}
          path: |
            desqemu-alpine-docker-${{ env.ALPINE_VERSION }}-${{ matrix.architecture }}.tar.gz
            quick-start-docker.sh
            quick-start-with-compose.sh
            DESQEMU-Alpine-README.md
          retention-days: 30

      - name: ⬆️ Rootfs → GitHub Artifacts | Upload rootfs to GitHub Artifacts
        uses: actions/upload-artifact@v4
        with:
          name: desqemu-alpine-rootfs-${{ env.ALPINE_VERSION }}-${{ matrix.architecture }}
          path: |
            desqemu-alpine-rootfs-${{ env.ALPINE_VERSION }}-${{ matrix.architecture }}.tar.gz
            quick-start-rootfs.sh
            quick-start-with-compose.sh
            DESQEMU-Alpine-README.md
          retention-days: 30

      - name: 🔧 Загрузка NBD модуля | Load NBD kernel module
        run: sudo modprobe nbd max_part=16

      - name: 🚀 Создание QCOW2 образа | Create QCOW2 image
        run: |
          echo "🚀 Запускаем сборку QCOW2 образа для ${{ matrix.architecture }}..."
          chmod +x scripts/create-alpine-qcow2.sh
          scripts/create-alpine-qcow2.sh ${{ matrix.architecture }} ${{ env.ALPINE_VERSION }}

      - name: ⬆️ QCOW2 образ → GitHub Artifacts | Upload QCOW2 image to GitHub Artifacts
        uses: actions/upload-artifact@v4
        with:
          name: desqemu-alpine-qcow2-${{ matrix.architecture }}
          path: |
            desqemu-alpine-qcow2-${{ matrix.architecture }}.qcow2
            quick-start-qcow2.sh
          retention-days: 30

      - name: 📦 Копирование QCOW2 в Desktop App | Copy QCOW2 to desktop app
        run: |
          echo "📦 Копируем QCOW2 образ в Desktop приложение..."
          
          if [ -f "desqemu-alpine-qcow2-${{ matrix.architecture }}.qcow2" ]; then
            # Создаем директорию если её нет
            mkdir -p desqemu-desktop/resources/qcow2
            
            # Копируем QCOW2 образ
            cp "desqemu-alpine-qcow2-${{ matrix.architecture }}.qcow2" "desqemu-desktop/resources/qcow2/alpine-bootable.qcow2"
            
            echo "✅ QCOW2 образ скопирован в Desktop App: $(du -h desqemu-desktop/resources/qcow2/alpine-bootable.qcow2 | cut -f1)"
            
            # Пересобираем desktop app с QCOW2 образом
            echo "🔄 Пересобираем Desktop приложение с QCOW2 образом..."
            cd desqemu-desktop
            neu build --release
            
            # Создаем финальный архив
            cd dist
            tar -czf "../../desqemu-desktop-linux-x64.tar.gz" .
            cd ..
            
            echo "✅ Desktop приложение пересобрано с QCOW2 образом"
          else
            echo "❌ QCOW2 образ не найден!"
          fi

      - name: 🚀 Создание портативного архива | Create portable archive
        run: |
          echo "🚀 Запускаем сборку портативного архива для ${{ matrix.architecture }}..."
          chmod +x scripts/build-portable.sh
          scripts/build-portable.sh ${{ matrix.architecture }}

      - name: 🛠️ Создание универсального установщика | Create universal installer
        run: |
          echo "🛠️ Создаем универсальный установщик..."
          chmod +x scripts/create-universal-installer.sh
          scripts/create-universal-installer.sh

      - name: ⬆️ Портативный архив → GitHub Artifacts | Upload portable archive to GitHub Artifacts
        uses: actions/upload-artifact@v4
        with:
          name: desqemu-portable-microvm-${{ matrix.architecture }}
          path: |
            desqemu-portable-microvm-${{ matrix.architecture }}.tar.gz
            install-desqemu-portable-${{ matrix.architecture }}.sh
            install-desqemu-portable.sh
          retention-days: 30

      - name: ⬆️ Release notes → Artifacts | Upload release notes to artifacts
        uses: actions/upload-artifact@v4
        with:
          name: release-notes
          path: release-notes.md
          retention-days: 7

      - name: 🔍 Проверка Desktop App | Check desktop app
        run: |
          echo "🔍 Проверяем Desktop App..."
          ls -la desqemu-desktop-linux-x64.tar.gz 2>/dev/null || echo "❌ Desktop App не найден"
          
          if [ -f "desqemu-desktop-linux-x64.tar.gz" ]; then
            echo "✅ Desktop App найден: $(du -h desqemu-desktop-linux-x64.tar.gz | cut -f1)"
          else
            echo "⚠️ Desktop App не найден, но это нормально - он создается в этом джобе"
          fi


      - name: 📋 Информация о созданных файлах | Information about created files
        run: |
          echo "📋 Созданные файлы:"
          ls -la *.qcow2 *.tar.gz *.sh *.md 2>/dev/null || echo "Файлы не найдены"
          
          echo ""
          echo "📊 Размеры файлов:"
          du -h *.qcow2 *.tar.gz 2>/dev/null || echo "Файлы не найдены"
          
          echo ""
          echo "✅ Все файлы созданы и загружены в GitHub Artifacts"
          echo "📥 Для скачивания: https://github.com/${{ github.repository }}/actions/runs/${{ github.run_id }}"

      - name: 🏷️ Создание GitHub Release | Create GitHub Release
        uses: softprops/action-gh-release@v1
        if: github.ref == 'refs/heads/master'
        with:
          name: "🚀 DESQEMU Alpine Linux ${{ env.ALPINE_VERSION }}"
          body_path: release-notes.md
          files: |
            desqemu-alpine-docker-${{ env.ALPINE_VERSION }}-${{ matrix.architecture }}.tar.gz
            desqemu-alpine-rootfs-${{ env.ALPINE_VERSION }}-${{ matrix.architecture }}.tar.gz
            desqemu-alpine-qcow2-${{ matrix.architecture }}.qcow2
            quick-start-docker.sh
            quick-start-rootfs.sh
            quick-start-qcow2.sh
            quick-start-with-compose.sh
            DESQEMU-Alpine-README.md
            desqemu-desktop-linux-x64.tar.gz
          tag_name: "v${{ env.ALPINE_VERSION }}-${{ github.run_number }}"
          draft: false
          prerelease: false
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}



  create-portable-release:
    name: Создание релиза портативных архивов
    runs-on: ubuntu-latest
    needs: [create-alpine-distribution]
    permissions:
      contents: write
    steps:
      - name: 📥 Получение кода репозитория | Get repository code
        uses: actions/checkout@v4

      - name: 📦 Скачивание всех артефактов | Download all artifacts
        uses: actions/download-artifact@v4
        with:
          pattern: |
            desqemu-portable-microvm-*
          merge-multiple: true

      - name: 📝 Создание release notes для портативных архивов | Create release notes for portable archives
        run: |
          echo "📝 Создаем release notes для портативных архивов..."
          chmod +x scripts/create-portable-release-notes.sh
          scripts/create-portable-release-notes.sh "${{ github.repository }}" "${{ github.ref_name }}" "${{ github.repository_owner }}" "${{ github.server_url }}"

      - name: 🏷️ Создание GitHub Release для портативных архивов | Create GitHub Release for portable archives
        uses: softprops/action-gh-release@v1
        if: github.ref == 'refs/heads/master'
        with:
          name: "🚀 DESQEMU Portable MicroVM ${{ env.ALPINE_VERSION }}"
          body_path: portable-release-notes.md
          files: |
            desqemu-portable-microvm-*.tar.gz
            install-desqemu-portable-*.sh
            install-desqemu-portable.sh
          tag_name: "portable-v${{ env.ALPINE_VERSION }}-${{ github.run_number }}"
          draft: false
          prerelease: false
          make_latest: false
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
